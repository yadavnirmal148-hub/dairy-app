# Netlify — after GitHub connect: Base directory = frontend
# Build: npm run build  |  Publish directory: build

[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
